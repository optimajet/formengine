/**
 * Option interface for selectable items with label and value.
 */
export interface IOption {
  /**
   * Display label for the option.
   */
  label: string
  /**
   * Value associated with the option.
   */
  value: string
}
