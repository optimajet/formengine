import {define} from '@react-form-builder/core'

interface RsCollectionEditorProps {

}

const RsCollectionEditor = (props: RsCollectionEditorProps) => {
  return (<>CollectionEditor</>)
}

export const rsCollectionEditor = define(RsCollectionEditor, 'RsCollectionEditor')
  .name('Collection editor')
